spring:
  datasource:
    url: jdbc:h2:mem:starticket;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate

app:
  infrastructure:
    enabled: false
  jwt:
    secret: test-secret-key-with-at-least-32-characters
