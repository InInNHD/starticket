global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: starticket
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["backend:8080"]
